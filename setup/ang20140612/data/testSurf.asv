clear;clc;
load surface.mat

ind = 5;

cov = squeeze(covs(ind,:,:));
cholcov = chol(cov);

figure();
plot(wl,means(ind,:), 'Linewidth', 2); hold on;

i = 0;
while i < 10
    samp = cholcov * normrnd(0,1,[432,1]) + means(ind,:)';
    
    if samp 
        plot(wl,samp)
end
